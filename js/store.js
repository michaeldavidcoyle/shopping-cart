import items from '../items.json';

const storeItemTemplate = document.querySelector('#store-item-template');

export function setupStore() {
    console.log(items);
    console.log(storeItemTemplate);
}